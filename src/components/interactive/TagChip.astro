---
interface Props {
  label: string;
  tone?: "default" | "success" | "neutral";
}

const { label, tone = "default" } = Astro.props;
---

<span class:list={["tag-chip", `tag-chip--${tone}`]}>{label}</span>
